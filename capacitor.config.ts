import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miapp.angular',
  appName: 'Sumativa',
  webDir: 'dist/myapp/browser'
};

export default config;
